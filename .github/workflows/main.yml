name: hello-world
on:
  push:
    branches:
      - main
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Run main.js
        run: node main.js
